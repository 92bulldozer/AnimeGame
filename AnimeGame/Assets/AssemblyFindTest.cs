
using UnityEngine;

public class AssemblyFindTest : MonoBehaviour
{
  
    //public VolumeParameter
    // Start is called before the first frame update
    void Start()
    {
        //PipelineAssetUtility.GetRenderer(asset)
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
